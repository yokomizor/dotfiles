# The fact the alias file is encrypted breaks mutt's create-alias function

echo "Decrypting aliases.gpg..."
source "gpg --decrypt --quiet --default-recipient-self ~/.mutt/aliases.gpg |"

set sort_alias = unsorted
set reverse_alias = yes
